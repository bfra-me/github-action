---
name: CodeQL

on:
  pull_request:
    types: [opened, synchronize]
  push:
    branches: [main, 'renovate/**']
  schedule:
    - cron: '31 7 * * 3' # Every Wednesday at 7:31am UTC

concurrency:
  group: ${{ github.workflow }}-${{ github.event.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  analyze:
    env:
      language: javascript
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - name: Initialize CodeQL
        uses: github/codeql-action/init@c503cb4fbb5ac9c4ff92454b2613f5bf931403e5 # v3.31.1
        with:
          languages: ${{ env.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@c503cb4fbb5ac9c4ff92454b2613f5bf931403e5 # v3.31.1

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@c503cb4fbb5ac9c4ff92454b2613f5bf931403e5 # v3.31.1
        with:
          category: '/language:${{ env.language }}'
